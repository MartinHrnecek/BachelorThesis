<Window x:Class="DesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Obfuscation Test App" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="IL Obfuscation Desktop Test" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="InputTextBox" Width="300" Margin="0,0,10,0"/>
            <Button Content="Analyze" Click="OnAnalyzeClick" Margin="0,0,5,0"/>
            <Button Content="Clear" Click="OnClearClick"/>
        </StackPanel>

        <ListBox x:Name="ResultsListBox" Grid.Row="2" Margin="0,0,0,10"/>

        <StatusBar Grid.Row="3">
            <TextBlock x:Name="StatusText" Text="Ready"/>
        </StatusBar>
    </Grid>
</Window>